# Language setting
language: java

# Version number
jdk:
  #- openjdk6
  #- openjdk7
  - oraclejdk7
  - oraclejdk8
  
matrix:
  include:
    - jdk: openjdk7
      env:
       - M1=one
    - jdk: openjdk6
    - jdk: oraclejdk8
    

build:
  ci:
    - java -version
